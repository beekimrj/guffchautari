
<%= simple_form_for @chatroom do |f| %>
	<%= f.input :name, label: "Chatroom Name", required: true %>
	<%= f.input :password, as: :string %>
 	<%= f.button :submit,class: "btn btn-primary" %>
<% end %>